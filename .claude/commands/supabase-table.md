Scaffold a Supabase table and its corresponding TypeScript integration for `$ARGUMENTS`.

Steps:
1. Write a SQL migration snippet (CREATE TABLE with RLS enabled) for `$ARGUMENTS`
2. Define the TypeScript type in `hello-world/lib/types/$ARGUMENTS.ts`
3. Create a Supabase query helper in `hello-world/lib/db/$ARGUMENTS.ts` with basic CRUD functions
4. Note any RLS policies needed

Output the SQL first so it can be run in the Supabase SQL editor.
