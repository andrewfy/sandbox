Create a new Next.js page at the path `$ARGUMENTS`.

Steps:
1. Create `hello-world/app/$ARGUMENTS/page.tsx` with a basic server component
2. Use Tailwind for styling, keep it consistent with the existing page
3. Export a `generateMetadata` function with a sensible title
4. Print the URL path where it will be accessible in dev

Keep the component simple — no unnecessary state or effects unless the user specifies.
